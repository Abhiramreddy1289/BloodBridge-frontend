# BloodBridge Frontend

React + Tailwind CSS frontend for the BloodBridge emergency blood connector platform.

## Setup

1. Change to the frontend directory:
   ```bash
   cd c:\BloodBridge\frontend
   ```
2. Install dependencies:
   ```bash
   npm install
   ```
3. Start the app:
   ```bash
   npm run dev
   ```
